# 架构评审 · 交付概览

**日期**：2026-09-02 · **范围**：claude_trading 全仓（28,563 行 / 80 源文件 / 433 测试）

## 做了什么

用 AST 解析出 `src/` 内部真实依赖拓扑，扫描跨文件重复实现，并实测运行全量测试，产出架构评审报告：

- 主报告：[`architecture_review.md`](./architecture_review.md)

## 核心结论

**这是一个"研究流水线"，却按"平台"的规模在组织。** 优化方向不是拆分，而是收敛。

生产逻辑集中在 4 个文件（约 3,112 行，占 11%），其余 89% 是三套并行体系与各自的横切实现。

## 三个必须先看的问题

1. **核心断裂**：`factors/` 通用因子框架被 `research/` 正常依赖，却被承载生产评分的 `scorer.py` 完全绕开（AST 确认无依赖边）。付了抽象的税，没享受抽象的好处。
2. **`scorer.py` 内部三路径**：legacy(337-386) / typed(388-466) / phase1(903-935)，加权逻辑与因子贡献各写两份，且 `_score_market`/`_score_technical` 的 `return 3` 把 legacy 语义漏进了 typed 路径。
3. **433 个测试实际跑不起来**：`responses` 缺失导致 pytest 收集阶段中断，0 个测试执行。绕过两个模块后全绿，但耗时 4m53s。**当前没有任何回归防线。**

## 建议的迁移路径

| 阶段 | 内容 | 收益 |
|---|---|---|
| **0 · 止血**（0.5天） | 恢复测试执行、网络测试隔离、删 6 个 0 引用依赖、删不存在的交易所配置 | 重建重构防线 |
| **1 · 三路径合一**（3-5天） | 维度注册表 + 通用加权器 + band 打分器 + 权重外置 YAML | 934 行 → ~200 行 |
| **2 · 数据源收口**（1周） | `BaseSource` 承担重试/限流/缓存/超时，10 个裸客户端迁移 | 12 客户端行为一致 |
| **3 · scripts 瘦身**（1周） | 抽 `theme.py`，4 个零依赖脚本逻辑下沉 | 消除三份重复 CSS |
| **4 · 因子融合**（可选） | 让 `scoring/` 消费 `factors/registry` | 彻底闭合断裂 |

阶段 0 必须先完成 —— **在此之前不要动 scorer**。

## 关键取舍

- **不拆微服务**：单人维护、无独立伸缩需求，先修模块边界
- **配置外置 YAML**：换取"改权重不发版"，放弃编译期类型检查，用 Pydantic schema 兜底
- **暂不融合 factors/**：阶段 1-3 先收敛各自内部，避免两套语义未理清时强行抽象

## 未决事项

阶段 4 需明确结论：要么融合 `factors/` 与 `scoring/`，要么删掉 `factors/` 中未被消费的部分。当前状态（建而不用）是纯负债。
